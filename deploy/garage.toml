# Garage object store — local dev config.
# replication_factor = 1: no replication (full disk capacity; cache is re-downloadable).
# The dev S3 keys (garage-init.sh) and the GARAGE_RPC_SECRET default are DEV-ONLY, override for production.

metadata_dir = "/var/lib/garage/meta"
data_dir = "/var/lib/garage/data"
db_engine = "sqlite"

replication_factor = 1

rpc_bind_addr = "[::]:3901"
rpc_public_addr = "garage:3901"
# rpc_secret is injected via the GARAGE_RPC_SECRET env var (see docker-compose.yml)

[s3_api]
s3_region = "garage"
api_bind_addr = "[::]:3900"
root_domain = ".s3.garage"

[s3_web]
bind_addr = "[::]:3902"
root_domain = ".web.garage"
index = "index.html"
