# Keep the high-value checks (SA bugs, U dead-code, S simplifications, QF); drop:
#   SA1019 — deprecated-symbol noise (aws manager.Uploader; migration is a tracked TODO)
#   ST1000 — "package should have a comment" (we keep comments minimal by choice)
#   ST1020 — exported-func comment phrasing (same reason)
checks = ["all", "-SA1019", "-ST1000", "-ST1020"]
